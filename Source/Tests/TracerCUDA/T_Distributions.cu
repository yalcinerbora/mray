#include <gtest/gtest.h>
#include <gmock/gmock-matchers.h>
#include <random>
#include <algorithm>

#include "Tracer/Distributions.h"
#include "GTestWrappers.h"


TEST(DistributionsTest, PiecewiseConstant1D_Basic)
{
    //std::vector<Float> data(500);

    //std::iota(data.begin(), data.end(), 0);
    //std::shuffle(data.begin(), data.end(), std::mt19937(123));
}